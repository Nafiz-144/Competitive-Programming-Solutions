#include <bits/stdc++.h>
using namespace std;
int main()
{
    int m, n, i, j;
    cin >> m >> n;
    vector<int> a, b, c;
    for (auto i = 0; i < a.size(); i++)
    {
        cin >> a[i];
    }
    for (auto k = 0; k < m + n; k++)
    {
        c = a[i] + b[i];
    }

    cout << c(begin().end()) << endl;

    return 0;
}