  vector<string> s;

        for (int i = 0; i < s.size(); ++i)
        {
            cin >> s[i];
        }
        for (int i = 0; i < s.size(); ++i)
        {
            if (i % 2 == 0)
            {
                cout << i << endl;
            }
        }